Recrear el sistema de scoreboards de minecraft, para ello voy a ocupar aprender los siguiente:

    1. aprender map en c++ para manejar la estructura comun de un scoreboard(usuario y puntos).
    2. aprender como hacer una base de datos y de que manera adaptarlo a como lo ocupo(.txt).

y seguir los siguientes pasos para lograrlo:

    1. crear la estructura para manejar el scoreboard
    2. crear funciones "add" y "remove" para manejar datos basicos del scoreboard
    3. crear una funcion para imprimir un diseño decente en terminal
    4. crear la funcion para leer/reescribir a un archivo(prob. txt) para guardar el scoreboard
    5. modificar la estructura del scoreboard para limitar esta misma y tener mejor control
    6. crear(de alguna manera) una tienda virtual para gastar los puntos

V.0.0.1-.
    - cree el map del scoreboard con el siguiente formato:
        "std::map <std::string nombre, long long score> scoreboard"
    - cree el primer diseño de impresion del score con el siguiente formato:
        "===========================
         nombre    :   score
         ==========================="

V.0.0.2-.
    -cree los archivos "funciones.h" y "funciones.cpp" para controlar los comandos
    -intente crear la funcion "add" pero no encuentro la manera de "modificar" el scoreboard
     desde el archivo "funciones.h" :p
    -modifique(porfin xd) el archivo "config.h" para declarar el map del scoreboard y sea "global"
    -logre que la funcion "add" modificara la variable "scoreboard" original
    -hice lo mismo para la funcion "remove"(copia y pega de add)
    -de una vez hice la funcion "mostrarScoreboard" con todo el diseño :p
    -pos valio, no ejecuta el proyecto :p
    -cambie el archivo "config.h" porque al parecer se declaraba varias veces una variable
    -cree el archivo "config.cpp" para separar la forma de existencia de variables
    -pos tampoco funciono, no ejecuta el proyecto
    -aprendi que para copilar y ejecutar correctamente mi proyecto, tengo que hacerlo por
     separado en terminal, primero compilo el proyecto con "g++ *.cpp -o main" y
     despues ejecuto "./main" :p

V.0.1.0-.
    -regrese despues de 2 semanas, a seguir con el proyecto :3
    -voy a crear una funcion para leer y escribir un archivo de texto para guardar los datos
     del scoreboard :3
    -encontre que la libreria <fstream> permite leer y escribir :3
    -cree la funcion de lectura llamada "leerScoreboard" este recibe la variable por referencia
     del scoreboard(acabo de aprender que las variables con & se les llama asi)
     la funcion abre el archivo "scoreboard.txt"
     la funcion comprueba que el archivo se abrio correctamente
     y este empieza a leer el contenido, al terminar cierra el archivo.
    -tambien cree la funcion de escritura llamada "escribirScoreboard" este recibe
     la variable por referencia constante del scoreboard 
     la funcion abre/crea el archivo "scoreboard.txt"
     y empieza a escribir/sobreescribir el contenido
     al terminar cierra el archivo
    -funciono todo a la perfeccion :3 (o mas o menos xd)

V.0.1.1-.
    -investigando un poco el comando scoreboard de minecraft mas exactamente los argumentos
     de "player" estos tienen comandos que pienso agregar:
     "add": ya esta puesto asi que nada, solo confirme el comportamiento con numeros negativos
            estos simplemente los resta.
     "remove": ya esta puestos asi que nada, solo confirme el comportamiento con numeros
            negativos estos simplemente los suma.
     "list": enlista todas las puntuaciones, asi que solo cambiaria el nombre de la funcion
            mostrarScoreboard a listScoreboard.
     "get": este regresa el punteje de un jugador.
     "set": establece el puntaje de un jugador, ya sea creandolo o sobreescribiendolo.
     "reset": borra el puntaje del jugador, borrando tambien el jugador del scoreboard.
    -hay otros pero no los considero necesarios.
    -asi que empezare a crear las funciones, y usar la terminal para ejecutar los comandos
    -cree todas la funciones nuevas, incluso modifique otras para mejor entendimiento
    -en el main.cpp puse texto de entrada para explicar brebemente el programa
    -luego la funcion de leerScoreboard por si existe el archivo
    -cree una variable string llamada salida con un texto defaul "holi"
     para controlar la entrada y salida del programa
    -cree una varible char llamada comando con el caracter 0
     para controlar los comandos y mostrar una lista de comandos
    -use un do while con un switch con 10 cases, usando el char comando para escoger
    -tarde en como resolver la salida del programa porque la opcion 9
     que es la que se encarga de cambiar la varianle salida
     porque al salir del switch este pedia un comando y no era logico que despues de pedir
     salir del programa pida un comando y depues salga del programa, estuve moviendo la
     entrada de comando hacia varias partes, hasta que por fin se me ocurrio un solucion obvia
     un if verificando la salida antes de pedir un comando xd
    -y ya es todo por esta version :3
    -mentira, probando el programa, al ejecutar el comando le leerScoreboard
     este no borra lo antes modificado un ejemplo:
     ===========================
     rogelio :       -600
     telo    :       960
     ===========================
     despues agrego a bera con 60 y borro a rogelio
     ===========================
     bera    :       60
     telo    :       960
     ===========================
     y vuelvo a leer el scoreboard, este no se borra, sino que se sobreescribe
     ===========================
     bera    :       60
     rogelio :       -600
     telo    :       960
     ===========================
    -voy arreglar eso
    -yap, agrege un .clear a la funcion de leerScoreboard para borrar todo el map
     para despues volverlo a llenar :3
    -ahora si creo que ya quedo esta version xd

V.0.2.0-.
    -se me olvido mencionar en la anterior version que la varible "nombre" fue cambiada por "jugador"
    -ahora voy hacer que el programa pueda crear/cambiar varios scoreboards
    -voy a borrar los archivos config.h y config.cpp porque ya no son necesarios
    -borrar el include de config en main.cpp
    -declarar la variable scoreboard en el main.cpp
    -voy a cambiar el texto de interfaz de la terminal para pedir abrir un archivo o 
     crear un archivo
    -ya cambie el texto ahora modificare la funciones leerScoreboard y escribirScoreboard
     para pedir un nombre de archivo, y que al nombre obtenido le agrege .txt
    -estoy considerando cambiar los nombres de la funciones leer y escribir a algo mas intuitivo
     como cargar y guardar, por ahora lo dejare asi.
    -ya modifique las funciones de leer y escribir
    -implemente 2 opciones de inicio, leer para abrir o leer un scoreboard ya existente
     y crear para escribir o crear un scoreboard nuevo
    -cambie la varible salir de string a bool para mejor entendimiento
    -cree la variable bool modificado para al momente de salir/cambiar preguntar si se desea guardar
     el scoreboard
    -puse la varible modificado = true cuando se modifique el scoreboard con un comando
     y modificado = false cuando se escriba/lea/cambie el scoreboard
    -el comando listScoreboard antes de imprimir el scoreboard, imprime el nombre del scoreboard
    -agrege la opcion/comando "c" para cambiar de scoreboard, si hay cambios en el scoreboard
     pregunta si se desea guardar
    -uy encontre un error en la opcion c, este solicita el nombre del scoreboard y luego lo lee...
     pero y si no existe?
    -cambie el el tipo de funcion de leerScoreboard a bool para verificar si existe o no el scoreboard
    -si devuelve false, borra el scoreboard y se crea uno nuevo
    -creo que ya esta, voy a probarlo :3
    -yes, no encontre ningun error :0